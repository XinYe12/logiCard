# Cross-Dept Contracts — Current Wave

**Wave:** none active as of this reset (2026-08-08). Wave 1+2 (Day 10 VFX, Day 11 audio, Ship docs) is fully
shipped and merged — no longer a live contract, see git history (`fc32a2d`, `a57d095`, `ef6e3f5`/`7e08aba`,
`04f9191`, `950ff63`) if you need the old signatures for reference.
**Updated:** 2026-08-08 by Integrator — reset for the **C46** scope pivot (see `docs/PRODUCT_MEMORY.md`
C46–C51, `docs/SCHEDULE.md`'s new phase table). The next wave of parallel work (Phase 1 landscape UI, Phase 2
networking foundation, or whatever the human/Integrator picks first) gets its own frozen contract here once
it's briefed.
**Rule:** Only Integrator edits this file after a merge. Workers implement against the frozen signatures
below.

## Frozen contracts this wave

*(none yet — add a section here per cross-dept interface, matching the shape of a past example: signature,
who owns which side, ART_DIRECTION/design pointer, wire-site notes, merge status.)*

## Ownership reminders this wave

*(populate once a wave starts — see `docs/departments/INDEX.md`'s ownership matrix for the same reset.)*
