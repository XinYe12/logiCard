# Draft Handoff — 2026-07-30 (late evening)

**Schedule position:** Day 3b done, C34 "Polished Core Demo" rescope in place (`docs/SCHEDULE.md`). Day 4 (M1 Slice 1, revised DoD: "ghost resolve + playback; Wound stub; cold-observer Move/Shoot/Time Card test — no new breadth") is next. **All code + the C34 doc rescope is committed and pushed** (`4d3d227`, `origin/master` in sync). This pass added a small, currently-**uncommitted** doc-consistency sync on top of that.

## Implemented

**Match lifecycle — C33 (committed at `4d3d227`)**
- `Timeline/MatchClock.cs` + `MatchSide.cs` — shared 900s pool, `TryPlayTimeCard` deducting N in full, chooser flip, `CanFundAnotherRound`.
- `RoundPhase` gained `Allot`/`Aftermath`/`MatchOver`; `PhaseStateMachine` is now cyclic; `RoundPhaseController` routes through Aftermath.
- `GameBootstrap` owns the `MatchClock` (`matchPoolSeconds = 900f`, `minRoundSeconds = 30f`); defender program rebuilds each Lock In from its carried tile + the round's N.
- `RoundPlayback.CommitRoundState()` snapshots end-of-tape tiles + wounds at Aftermath; wounds resume across rounds (no surcharge/bleed yet — deferred under C34).
- `ProgramHud` — Allot panel (`TimeCardPresets = {30, 60, 120}` + ALL IN + custom slider), Aftermath panel (NEXT ROUND / MATCH OVER), match-remaining readout.

**Day 4 ghost resolve (committed at `4d3d227`)**
`Net/GhostResolver.cs`, `ReplayTape.cs`, `TapeEvent.cs`, `Sim/GridLineOfSight.cs`, `Boot/RoundPlayback.cs`, `Board/ShotTracerView.cs`. Design rationale in `docs/DAY4_GHOST_RESOLVER_RESEARCH.md`.

**Product rescope — C34 Polished Core Demo (committed at `4d3d227`)**
`PRODUCT_MEMORY.md` (C34 + C5/C6/C17/C20/C29 annotated), `GDD.md` v0.2, `SCOPE.md`, `VERTICAL_SLICE.md`, `CORE_LOOP.md`, `ART_DIRECTION.md` (required art floor), `SCHEDULE.md` (Days 4–7 core combat, 8–11 diorama art, 12–14 ship).

**This pass — doc-consistency sync (uncommitted, see Blockers)**
Followed up a design-doc scan that found `GDD.md`/`SCOPE.md` still describing the pre-C33 model; by the time this pass started that big contradiction was already fixed by the C34 rewrite above. Closed the two remaining gaps:
- `docs/GDD.md` §6: added the Time Card preset ladder as a numeric row (was code-only).
- `docs/PRODUCT_MEMORY.md` C33 + `.cursor/rules/logicard-product-memory.mdc`: appended MinRoundSeconds=30s and the preset ladder into the CONFIRMED text itself, not just GDD.
- `docs/SCHEDULE.md`: added an explicit line stating Day 3b is absorbed within the existing Days 2–4 (M1) window, not a 15th calendar day — Day 14 ship date unchanged. Numbers were cross-checked against `GameBootstrap.cs`/`ProgramHud.cs` before writing them down, not copied from an earlier note.

## Verification

- Last known run (pre-dates this doc-only pass): Unity batch-mode suites, **EditMode 61/61**, **PlayMode 20/20**, clean compile — includes `MatchClockTests` and a PlayMode regression that plays a Time Card, locks in, reaches Aftermath, starts round 2, and confirms board input works again from carried tiles.
- **Not verified:** manual in-Editor play of the Allot → Program → Reveal → Execute → Aftermath loop by hand. Automated tests pass; nobody has watched it run yet.
- This pass (doc edits only) needed no test run.

## Still unfinished

- Manual in-Editor playtest of the full round-to-round loop (Time Card presets, carried positions/wounds, chooser alternation) — do this before trusting the automated-test coverage blind.
- `Assets/_Project/Scenes/Bootstrap.unity` may still serialize stale removed fields (`timeResourceBudgetSeconds`, `playbackSecondsForFullBudget` from before the `MatchClock` refactor) — Unity falls back to C# defaults so behavior is correct, but worth re-saving the scene to drop orphaned keys.
- Day 4 unchecked in `SCHEDULE.md` — code exists, but the cold-observer DoD pass hasn't been done.
- C34 core-combat scope not started: real path-drawing + stance bands (stance still hardcoded Walk), Hold Angle (`ShootCost.HoldAngleSeconds` exists, unused), the one door.
- No C34 art pass yet — still primitives, Built-in render pipeline (Unity Hub flags it deprecated; URP migration is scheduled Day 8, not an incidental fix).
- Today's doc-sync edits are uncommitted (see Blockers).

## Tomorrow

1. Manual playtest pass first (Time Card → Program → Lock In → Aftermath → NEXT ROUND, twice) — closes the Day 4 cold-observer DoD.
2. Re-save `Bootstrap.unity` to clear any stale serialized fields.
3. Confirm Day 4's DoD against what's already built; tick the checkbox only if the cold-observer pass actually reads correctly.
4. Day 5 (path drawing + stance bands) per the revised schedule — do not start Fusion, gear cards, the attic, or the art pass early.

## Blockers / notes

- Only today's doc-sync (`GDD.md`, `PRODUCT_MEMORY.md`, `.cursor/rules/logicard-product-memory.mdc`, `SCHEDULE.md`) is uncommitted; everything else is on `origin/master` at `4d3d227`. Nothing here should be controversial to commit as-is — it's numbers that were already true in code, now written into CONFIRMED docs, plus one clarifying schedule sentence.
- Batch-mode test runs need an isolated copy of the project directory while the Editor has the real one open, otherwise Unity aborts with a project-lock error.
