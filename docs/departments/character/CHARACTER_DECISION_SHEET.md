# Character — Human Decision Sheet (C42 / C43 / C44 / Attrs)

**Status:** **Fully answered 2026-08-16.** Every row across Part A–D now has a Decision, all accepting
the Agent recommendation column as-is (human chose "accept all" over a row-by-row walk). Promoted to
`PRODUCT_MEMORY.md` as **C70** (Part A), **C71** (Part B / Bomber), **C72** (Part C / Time Player),
**C73** (Part D / attrs) — see those rows for the consolidated decisions; this sheet stays the detailed
record. **Nothing here greenlights code** — these are design decisions, not a contract; Integrator opens
the actual C36/Bomber/Time Player/attrs Sim contracts separately, sequenced per A1/B9/C1's prerequisites.
**Purpose:** Every OPEN question from the four Character implementation briefs, in one pass, so the
human can answer once instead of re-reading four docs. **Nothing here is locked.** Recommended
defaults are carried over verbatim from each brief's own "recommendation, not locked" language —
this sheet does not invent new numerics or resolve-shape opinions. The **Agent recommendation** column
(added 2026-08-15) is the agent's own reasoned pick per row, building on that brief language where it
exists and on cross-referenced docs (`CHARACTER_C36_DEPENDENCY.md`, `CHARACTER_DETONATOR_VS_BOMBER.md`)
where no default was offered — it is a recommendation, not a decision; the Decision column is still
the human's alone.
**What happens after:** Human fills in "Decision" column (or writes free text) → Integrator promotes
answers into `PRODUCT_MEMORY.md` as C-numbered rows → Integrator opens the relevant Sim-pause
carve-out(s) → Character/UI contracts can open against frozen signatures.
**Out of scope for this sheet:** two other open items already tracked on
[`STATUS.md`](STATUS.md) — `CHARACTER_FANTASY.md` §6 (roster naming/pitches) and
`CHARACTER_TIME_PLAYER_EPISTEMICS.md` §4 (same fast-forward question as Part C Q1 below, from a
narrative-framing angle) — answer those alongside this sheet if convenient, but they aren't
re-derived here.

---

## Part A — Shared gates for *any* unique verb (C42, applies to Bomber + Time Player both)

| # | Question | Recommended default (brief's own words) | Agent recommendation (not locked) | Decision |
|---|----------|------------------------------------------|------------------------------------|----------|
| A1 | **Promotion gate.** Which `SCHEDULE.md` phase (or Integrator carve-out) is allowed to open the **first** unique-verb Sim contract — and is that Bomber, Time Player, or a thinner vertical slice (e.g. wall-only geometry breach without floor-drop)? | *No default offered — this is the human's call to sequence.* | — already decided by human. | **Bomber first** (2026-08-14). |
| A2 | **Data shape on Character.** How does a Character declare its unique verb(s)? | Not decided between: (a) ScriptableObject ability-id list on `CharacterData`, (b) hardcoded archetype→verb map in Boot, (c) separate `CharacterAbility` assets. Brief leans toward (a) for consistency with existing asset-driven attrs but does not lock it. | — already decided by human. | **(b) Hardcoded archetype→verb map in Boot** (2026-08-14). |
| A3 | **`ActionVerb` vs parallel channel.** Grow the `ActionVerb` enum (one value per unique verb, e.g. `BombAttach`) vs. a side-band "ability node" list on the payload? | **Grow `ActionVerb`** — matches existing Door-style verb-driven resolve, keeps Host sort/filter simple, avoids inventing a second timeline. | **Agree — grow `ActionVerb`.** Tests already assert verb-specific tape events and Host already sorts/filters by verb; a parallel channel would need every Program/Resolve/Tape path to learn a second thing to check, for no gain since wire format stays Host-revalidatable either way. | **Grow `ActionVerb`** (2026-08-15, accepted agent rec). |
| A4 | **Program HUD affordance ownership.** When Character (rules) and UI (chrome) are both hot on the same unique-verb slice, who owns the mode button / board-anchored prompt? | **Integrator freezes the node signature → Character proposes resolve → UI implements Program-arm UI against the frozen signature** (same split as Bandage). | **Agree.** This split already shipped once for Bandage and the 2026-08-13 mandate shift already handed Char Select chrome to UI — reusing the same seam avoids re-litigating a boundary that's already proven out. | **Integrator freezes signature → Character proposes resolve → UI builds chrome** (2026-08-15, accepted agent rec). |
| A5 | **Monetization check at promotion.** | Explicit Integrator+human checklist item at merge time: confirm the unlock path is free/skill-gated before merge — don't leave it to store copy later. *(Process confirmation, not a design fork — flag if you want it handled differently.)* | **Agree, and make it a literal checklist line in the contract template**, not prose in a brief. The no-pay-to-win rule (C47) is already locked; the risk isn't disagreement, it's someone forgetting to check at merge time under deadline pressure. | **Yes — explicit checklist line at merge** (2026-08-15, accepted agent rec). |

**Prerequisite the human should see before answering A1:** per C42 §4, the recommended build order is
(1) freeze this Part A, (2) land C36 geometry-breach primitives (or a scoped subset — both Bomber and
Time Player depend on it), (3) then open Bomber and/or Time Player contracts, (4) per-floor occupancy
only if Bomber's floor-drop is in the same contract. C36 itself is not one of these four briefs and has
its own OPEN #6 in `PRODUCT_MEMORY.md`.

---

## Part B — Bomber (C43)

| # | Question | Recommended default (brief's own words) | Agent recommendation (not locked) | Decision |
|---|----------|------------------------------------------|------------------------------------|----------|
| B1 | **Attach vs detonate — one node or two?** Roster assumes two scheduled events (mirrors Door Open/Close). If two: can another Character's Move/Shoot interact with an attached-but-live bomb? Can Time Player rewind an attached bomb? | Two nodes (assumed, not confirmed). Interaction sub-questions have **no default offered**. | **Two nodes**, confirming the roster assumption — Door Open/Close is the closest live precedent and the brief's own §4 already writes Attach/Detonate as separate nodes. **Other Characters cannot interact with a live attached bomb in v1** (no Move/Shoot rule needed yet — simplest resolve). **Time Player cannot rewind an attached bomb in v1** — consistent with C7's own default (Time Player v1 = geometry breach states only, bombs are out of scope). | **Two nodes; no cross-Character interaction with a live bomb; Time Player cannot rewind it in v1.** (2026-08-16, accepted agent rec) |
| B2 | **Cost strawman.** | Attach ~3s, Detonate ~1s — **recommendation only**, replace freely. | **Accept as placeholder.** Numerics are explicitly OPEN #9 for playtest tuning (same treatment as every other Character numeric per C20/C25); the arm-costs-more-than-trigger shape mirrors Door's own asymmetric Open/Close feel. | **Attach ~3s / Detonate ~1s, placeholder for playtest tuning.** (2026-08-16, accepted agent rec) |
| B3 | **Blast footprint.** Exact attached-surface footprint only, or a radius that can wound/breach adjacent points? | Footprint-only — the floor-drop rule as written assumes footprint, not radius (radius would expand collision tests). | **Agree — footprint-only for v1.** A radius model is a second geometry system on top of a C36 primitive that doesn't exist in code yet; prove footprint-only first, expand later if playtest wants area denial. | **Footprint-only for v1.** (2026-08-16, accepted agent rec) |
| B4 | **Attach eligibility.** Every floor/wall sample vs. designed breach points only. | **Designed points only** — matches C36's scoped destruction, avoids freeform mesh cutting. | **Agree.** Designed points only is already locked at the C36 level (scoped destruction, not freeform), and it's also what makes board-anchored UI possible at all — `UI_BOARD_ANCHORED_COMPONENTS.md` needs an enumerable, identifiable set of points to show identity/state on, which "every wall sample" can't give it. | **Designed breach points only.** (2026-08-16, accepted agent rec) |
| B5 | **Walls too, or floor-drop only?** If walls-only ships first, floor-drop and per-floor infra can defer. | *No default offered* — brief flags that a walls-only-forever Character may not deserve the "Bomber" name; human call. | **Ship wall-only first, defer floor-drop.** Per-floor occupancy doesn't exist at all today (C39 item 6, not a numerics gap) — wall-only is the only slice that's actually buildable before that infra lands, independent of the naming question. Treat "does wall-only still deserve the name Bomber" as a `CHARACTER_FANTASY.md` §6 branding question, not a build-sequencing blocker. | **Ship wall-only first; floor-drop deferred until per-floor infra exists.** (2026-08-16, accepted agent rec) |
| B6 | **Fall transition.** Instant teleport to equivalent XY one floor down vs. scheduled fall duration; does fall cancel the victim's remaining queue (mirrors the death-freeze precedent); can landing wound? | *No default offered* on any of the three sub-questions. | **Instant teleport to equivalent XY** — avoids inventing a new timed-fall animation and Playback presenter before the base breach mechanic even exists. **Fall cancels the remaining queue** — reuse the death-freeze (C37) precedent directly rather than designing a new interrupt rule from scratch. **Landing does not wound in v1** — a second wound source multiplies the balance surface before the base drop mechanic is proven; add later if the fantasy needs it. | **Instant teleport; fall cancels remaining queue (C37 precedent); landing does not wound in v1.** (2026-08-16, accepted agent rec) |
| B7 | **Who can be dropped?** Enemy only, any pawn (incl. self/ally), objects? | Brief leans toward "drop is a geometry consequence, not a targeted attack" (aligns with blind-programming / free-aim philosophy) — implies **any pawn in the footprint**, not enemy-only, but this is inference, not a stated default. | **Agree with the brief's inference: any pawn in the footprint, including self/ally.** Bomber has no pawn-id targeting anywhere in its design (blind program, free-aim family per C32/C39) — carving out an "enemy only" exception would require inventing an aim mechanic this Character otherwise doesn't have, just for this one interaction. | **Any pawn in the footprint, including self/ally — no targeting.** (2026-08-16, accepted agent rec) |
| B8 | **Cross-round state.** Do attached-undetonated bombs and geometry breach state carry across rounds like wounds (C33/C36), or must a bomb detonate the same round it's attached? | *No default offered.* | **Persist across rounds, like breach state and wounds.** C36 breach state already carries across rounds (locked); a same-round-only expiry on bombs would be a novel mechanic nothing else in the game has, and match-persistence is the simpler rule to implement and to explain to a player. | **Persist across rounds, same as breach state / wounds.** (2026-08-16, accepted agent rec) |
| B9 | **Prerequisite ordering.** Must C36 geometry breach merge before any Bomber contract opens, or may one Integrator contract deliver "breach points + Bomber verb" together? | *No default offered* — Integrator sequencing call, informed by A1. | **Bundle is fine for the wall-only slice** — `CHARACTER_C36_DEPENDENCY.md` §4A ("C36 enough" checklist) is the real gate, not a separate-merge requirement, and one contract avoids a merge-order stall. **Floor-drop still needs its own separate per-floor-infra carve-out** regardless (per B5) — don't fold that into the same contract. | **One bundled contract OK for wall-only slice; floor-drop stays a separate later carve-out.** (2026-08-16, accepted agent rec) |

---

## Part C — Time Player (C44)

| # | Question | Recommended default (brief's own words) | Agent recommendation (not locked) | Decision |
|---|----------|------------------------------------------|------------------------------------|----------|
| C1 | **Fast-forward survival — blocks buildability, answer this first.** | Four options: **(A)** rewind only, cut FF for v1. **(B)** FF allowed but only for deterministic transitions already scheduled by public board rules or the Time Player's own nodes — never peeks opponent payload. **(C)** FF only as a resolve-time booking (no Program-time preview), same info-opacity as booking a Door open. **(D)** other, write into PRODUCT_MEMORY once confirmed. Brief recommends **(A) or (C)** to start discussion — flags (B) as easy to get wrong in HUD copy. | — already decided by human. | **C** — FF only as resolve-time booking; no Program-time preview (2026-08-14). |
| C2 | **Cost strawman.** | Rewind ~4s / Fast-forward ~4s if it survives C1 — **recommendation only**. | **Accept as placeholder** — same playtest-tuning treatment as every other Character numeric (OPEN #10, C20/C25 precedent). | **Rewind ~4s, placeholder for playtest tuning.** (2026-08-16, accepted agent rec) |
| C3 | **Range / LoS.** Unrestricted vs. Door-like `InteractRadius` vs. Shoot-like LoS to the object. | **Radius interact (Door family)** — keeps it an object verb, not a sniper verb. | **Agree.** Door's `InteractRadius` gate is already live code (`TryQueueDoor`) to copy from directly; Shoot-like LoS would need new raycast/obstacle math this verb doesn't otherwise need, for a fantasy that's about proximity to an object, not sightline to it. | **Door-style `InteractRadius` gate.** (2026-08-16, accepted agent rec) |
| C4 | **Object scope v1.** Breach geometry only vs. also Standard/Vent doors vs. attached Bomber bombs vs. C38 pawn states. | **Geometry breach states only for v1** — Door rewind collides with Door UX expectations, pawn-state rewind collides with C38 revive design. | **Agree.** Breach-only is also the only object type that already needs a state-machine `IStatefulBoardObject`-style primitive built for Bomber (per `CHARACTER_C36_DEPENDENCY.md` §4A) — Time Player reusing it needs zero new object-type plumbing, where Door/bomb/pawn scope each drag in a separate system's UX rules. | **Geometry breach states only for v1.** (2026-08-16, accepted agent rec) |
| C5 | **What "timeline" means.** Discrete ±1 state step vs. replay of the object's last N match events. | **±1 along the defined state machine** — no general rewind clock. | **Agree.** A general rewind clock would need an event-replay log nothing else in Sim keeps, and would jeopardize the Host's bit-identical-math guarantee; ±1 on the same enum C36 already defines is the smallest change that delivers the "un-breach a wall" fantasy. | **±1 discrete state step, no general rewind clock.** (2026-08-16, accepted agent rec) |
| C6 | **Illegal targets.** Already-Intact (rewind) / already-terminal (FF) objects; object mid-transition from another node the same second — Program-time gate (can't even queue it) vs. resolve-time no-op? | *No default offered.* | **Program-time gate, not a resolve-time no-op.** `TryQueueDoor`'s radius gate already establishes queue-time legality checks as the house style, and a silent resolve-time no-op means a player only discovers their booked action did nothing after Playback — worse HUD feedback than Door already gives today. | **Program-time gate (queue-time legality check).** (2026-08-16, accepted agent rec) |
| C7 | **Interaction with Bomber** (only matters if both Characters ship). Can Time Player rewind a floor mid-fall? Rewind an attached-but-undetonated bomb? | **Default: out of v1 scope** — flagged as cross-brief, not designed here. | **Agree — out of v1 scope.** Both Characters are already sequenced as separate contract waves (`CHARACTER_DETONATOR_VS_BOMBER.md` §7 keeps C38/C43 separate the same way); designing their interaction now means designing for a roster combination that may not even ship in the same phase. | **Out of v1 scope.** (2026-08-16, accepted agent rec) |

**Prerequisite:** per C44 §4, Time Player's primary example cannot ship before C36 breach primitives
exist (same as Bomber). Do not open Time Player against doors-only as a substitute fantasy unless the
human explicitly re-scopes the Character.

---

## Part D — Scout / Juggernaut attrs (current cast, *not* a unique-verb operator)

| # | Question | Recommended default (brief's own words) | Agent recommendation (not locked) | Decision |
|---|----------|------------------------------------------|------------------------------------|----------|
| D1 | **Doc numeric authority.** GDD §2 (1.5 / 0.75 units/s) and §6 (1.0s / 2.0s per unit) disagree — reciprocals only if Walk baseline is the rate, but §6's 1.0s/unit ≡ 1.0 u/s, not 1.5. Which wins? | **§6 + the live assets win** (1s / 2s per unit) — Integrator amends §2 on confirm. | **Agree — §6 + assets win.** The live `Scout.asset`/`Juggernaut.asset` already encode §6's numbers, so picking §2 instead would mean changing shipped assets to match a doc rather than fixing the doc to match what's actually running; §6 is also the more specific/derived table, §2 reads as the stale summary. | **§6 + live assets win; Integrator amends GDD §2.** (2026-08-16, accepted agent rec) |
| D2 | **Wire Agility now or later?** C25 (stance-change / Snap↔Hold switch penalty) is design-locked but the fields are read nowhere in `Assets/_Project` except the ScriptableObject assets themselves. Is fixing that a narrow bug-fix Sim carve-out, or does it wait for a full Character-attrs contract? | *No default offered* — flagged only as "confirm before treating as a quick fix," since attrs wiring is Sim/Timeline-adjacent and needs an explicit carve-out either way. | **Park it for a full Character-attrs contract, don't treat as a quick fix.** The brief's own §4 bundles Agility wiring with `PawnProgram` ctor changes and door-cost sourcing into one shape — doing Agility alone as a "bug fix" risks a second pass touching the same deterministic resolve code under the general Sim pause, which is exactly the kind of narrow carve-out this project has already been burned by skipping process on. | **Full Character-attrs contract, not a quick-fix carve-out.** (2026-08-16, accepted agent rec) |
| D3 | **When does the penalty apply?** Sprint-only vs. any stance change; Snap↔Hold every switch vs. only the first switch per round. | C25 says "once when switching" / "same shape as the stance-change penalty" — brief's reading: **charge each switch event that matches the rule, not once per match.** Confirm this reading. | **Agree with the brief's reading — charge every switch event, not once per match.** Every other Program cost (Move, Shoot, Door) charges per action instance, not per match; a once-per-match cap would need new bookkeeping nothing else in `PawnProgram` has, for a reading of "once when switching" that more naturally means "once per switch event" anyway. | **Charge every qualifying switch event, not once per match.** (2026-08-16, accepted agent rec) |
| D4 | **Character Select → live attrs.** Does `SelectedArchetype` actually drive `PawnProgram` construction for the local player? For a local 2nd pawn / scripted defender? For future net opponents? | *No default offered* — brief flags this as an unaudited wiring gap (`GameBootstrap` currently hardcodes attacker/defender speeds rather than loading `Scout.asset`/`Juggernaut.asset` by reference). | **Audit and fix for the local player inside the same D2 attrs contract; scope 2nd-pawn/net-opponent wiring explicitly rather than assuming it's covered.** The brief's own repo read found `GameBootstrap` hardcodes speeds today, meaning Character Select may currently be cosmetic-only for the one path that matters most (local player) — that's a correctness gap worth closing now, but scripted-defender and future net-opponent spawn paths may use different construction code and shouldn't be assumed fixed by the same change without checking. | **Fix local-player wiring inside the D2 attrs contract; scope 2nd-pawn/net-opponent separately, not assumed.** (2026-08-16, accepted agent rec) |
| D5 | **Vent / Breach doors and Strength.** Do Vent/Breach doors use the same `doorInteractBaseSeconds` as Standard doors, or kind-specific costs? | *No default offered* — map feature, but Strength is Character-owned, so flagged here. | **Default to the same `doorInteractBaseSeconds` for all door kinds in v1.** Nothing in the locked design (GDD §2/§6, C25) calls for kind-specific Strength costs, and `DoorKind.Breach` is already repeatedly flagged across these briefs as a naming trap to keep simple, not a place to hang new numeric complexity — introduce a Vent/Breach-specific cost only if a future map/door brief has a concrete reason to want the kinds to feel different. | **Same `doorInteractBaseSeconds` for all door kinds in v1.** (2026-08-16, accepted agent rec) |
| D6 | **Interact-as-card Strength carve-out (C62).** Not this brief's implement scope — but should a future attrs contract be constrained not to rename/remove `doorInteractBaseSeconds` in a way that blocks that later gear hook? | Brief's stance: **yes, preserve the field/shape** so C62's later hook isn't blocked. Flagging for explicit sign-off since it constrains implementation freedom now for a not-yet-built feature. | **Agree — preserve the field/shape.** The cost of keeping `doorInteractBaseSeconds` stable now is zero (it's already the field the attrs contract needs to wire correctly per D2/D4 anyway); foreclosing C62's already-locked future gear hook to save a rename later is the wrong trade. | **Preserve `doorInteractBaseSeconds` field/shape for C62's future gear hook.** (2026-08-16, accepted agent rec) |

---

## Not covered here (already answered, or tracked elsewhere — do not re-litigate)

- Unique verbs resolve through the deterministic Host event-stream, no physics calls (C42, locked).
- Unique-verb Characters ship free/skill-gated, never paywalled (C42/monetization guardrail, locked).
- Bomber/Time Player get no exclusive gear — their difference is the verb (C62, locked).
- `DoorKind.Breach` (map shortcut door) is unrelated to C36 geometry breach — naming collision only,
  not a design question (all three briefs flag this).
- Scout/Juggernaut share Move/Shoot/Door; only attr magnitudes differ (GDD §2/§6, locked — see D1 for
  the one remaining numeric-authority conflict).

---

## See also

- [`CHARACTER_UNIQUE_VERB_OPERATORS_BRIEF.md`](../../CHARACTER_UNIQUE_VERB_OPERATORS_BRIEF.md) — Part A source
- [`CHARACTER_BOMBER_AGENT_BRIEF.md`](../../CHARACTER_BOMBER_AGENT_BRIEF.md) — Part B source
- [`CHARACTER_TIME_PLAYER_AGENT_BRIEF.md`](../../CHARACTER_TIME_PLAYER_AGENT_BRIEF.md) — Part C source
- [`CHARACTER_ATTRS_SCOUT_JUGGERNAUT_BRIEF.md`](../../CHARACTER_ATTRS_SCOUT_JUGGERNAUT_BRIEF.md) — Part D source
- [`STATUS.md`](STATUS.md) — other outstanding human-facing items (Fantasy §6, Epistemics §4)
- `PRODUCT_MEMORY.md` — where confirmed answers get promoted to C-numbered rows
