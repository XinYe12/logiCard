namespace LogiCard.Timeline
{
    /// <summary>
    /// Attacker / Defender labels for spawn and for who plays the Time Card this round (C33).
    /// Resolve stays simultaneous — only the allotment choice alternates.
    /// </summary>
    public enum MatchSide
    {
        Attacker = 0,
        Defender = 1,
    }
}
